import React from "react";
import styled from "styled-components";

const SearchResultsContainer = () => {
	return (
		<div>
			<p> this is the SearchResultsContainer </p>
		</div>
	);
};

export default SearchResultsContainer;
