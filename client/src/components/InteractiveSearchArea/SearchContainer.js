import React from "react";
import styled from "styled-components";

const InteractiveSearchArea = () => {
	return (
		<div>
			<p> this is the InteractiveSearchContainer </p>
		</div>
	);
};

export default InteractiveSearchArea;
