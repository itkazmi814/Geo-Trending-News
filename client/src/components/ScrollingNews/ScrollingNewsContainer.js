import React from "react";
import styled from "styled-components";

const ScrollingNewsContainer = () => {
	return (
		<div>
			<p> this is the ScrollingNewsContainer </p>
		</div>
	);
};

export default ScrollingNewsContainer;
